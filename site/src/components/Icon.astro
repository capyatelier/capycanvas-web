---
import { paths } from '../lib/icons';
interface Props {
  name: keyof typeof paths;
  class?: string;
}
const { name, class: className } = Astro.props;
---
<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" class={className} set:html={paths[name]} />
